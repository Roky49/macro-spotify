services:
  api:
    build: .
    ports:
      - "5480:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - DOWNLOAD_DIR=/downloads
    volumes:
      - ./downloads:/downloads
    restart: unless-stopped
